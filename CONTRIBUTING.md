# 参与贡献

感谢参与 LingXiCode。

## 开始之前

1. 从公开仓库创建分支。
2. 使用 `npm ci` 安装依赖。
3. 不要复制个人开发目录中的 `data/`、`.lingxi/`、本机配置或聊天记录。
4. 不要提交 API Key、Cookie、访问令牌、账号信息或私人绝对路径。

## 提交前检查

```powershell
npm run check
npm run test:scenarios
git status --short
git grep -n -E 'sk-[A-Za-z0-9_-]{20,}'
```

新增功能应附带对应场景测试。修复 UI 状态问题时，应覆盖项目切换、会话切换和运行中恢复等真实时序。

## 资源和第三方代码

只有在你确认有权按照兼容许可证再分发时，才能提交图片、音频、视频、字体、模型、插件或复制来的组件。请同时提交许可证和来源说明。

## 提交内容

提交 Pull Request 时请说明：

- 修改了什么
- 为什么修改
- 如何验证
- 是否新增依赖、权限、网络请求或第三方资源

